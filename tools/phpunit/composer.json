{
    "require": {
        "symfony/phpunit-bridge": "^6.1",
        "rregeer/phpunit-coverage-check": "^0.3.1"
    },
    "scripts": {
        "post-install-cmd": [
            "@php vendor/bin/simple-phpunit install"
        ],
        "post-update-cmd": [
            "@php vendor/bin/simple-phpunit install"
        ]
    }
}
